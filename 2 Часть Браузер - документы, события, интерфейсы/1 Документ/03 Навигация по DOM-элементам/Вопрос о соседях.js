/* Если elem – произвольный узел DOM-элемента…

Правда, что elem.lastChild.nextSibling всегда равен null?
Правда, что elem.children[0].previousSibling всегда равен null ? */

//1)если имеются комментарии или пробелы, то нет, если их нет, то да
//2)аналогично выше, если есть комментарии или пробелы, то нет, если их нет, то да